import { mdLinks } from "../components/mdLinks";

describe('mdLinks', () => {

  it('Deberia retornar un arreglo con 3 objetos si es un archivo md', () => {
    return mdLinks('components/05-milestone.md').then(result) =>
    expect(res);
  });

});
